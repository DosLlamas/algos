#include <stdio.h>
#include <stdlib.h>

int main(){
    const int MAX_LENGTH = 101; // Limits input to 101 char, including the null terminator
    char buffer[MAX_LENGTH];
    // Read input then print
    // fgets(buffer, MAX_LENGTH, stdin);
    // printf("%.3f\n", "hil");
    return 0;
}